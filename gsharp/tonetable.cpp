#include "headers/gsheaders.h"

GSharpStructs::ToneTable ETable;

// I can create a tuple that accepts key/chord/tone as parameters and returns the corresponding values and all of their additional values -- make callable individually and as a group
	// need to group and organize data 
	// need to make objects that can return all some or none of the values according to the parameters provided
int main(){
cout << ETable.BO9 << endl;
cout << ETable.fretBoard[0][0] << endl;
return 0;
}
